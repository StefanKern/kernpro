// Initialize Angular TestBed for zoneless testing via jest-preset-angular
// Using CommonJS require to avoid ESM loader issues
// eslint-disable-next-line @typescript-eslint/no-var-requires
require('jest-preset-angular/setup-env/zoneless').setupZonelessTestEnv();
